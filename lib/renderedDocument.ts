export type RenderedDocument = {
    id: string,
    content: string,
    title: string,
    date: string,
    card?: string,
    url?: string,
    type?: string,
    where?: string,
    rank?: string,
    mastodonId?: string,
}